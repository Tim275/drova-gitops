## After ArgoCD finishes syncing production, POST a repository_dispatch event
## to drova so prod-verify.yaml workflow auto-runs (health-poll + auto-rollback).
##
## Closes the "manual-only" gap: production deploys now self-verify.
apiVersion: batch/v1
kind: Job
metadata:
  name: drova-dispatch-prod-deployed
  annotations:
    argocd.argoproj.io/sync-wave: "100"
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
spec:
  ttlSecondsAfterFinished: 300
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: dispatch
          image: curlimages/curl:8.10.1
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          env:
            - name: GH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: drova-dispatch-token
                  key: GH_TOKEN
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              TIMESTAMP=$(date -u +%FT%TZ)
              echo "Sending production-deployed event @ ${TIMESTAMP}"
              curl -sf -X POST \
                -H "Accept: application/vnd.github+json" \
                -H "Authorization: Bearer ${GH_TOKEN}" \
                -H "X-GitHub-Api-Version: 2022-11-28" \
                "https://api.github.com/repos/Tim275/drova/dispatches" \
                -d '{"event_type":"production-deployed","client_payload":{"deployed_at":"'"${TIMESTAMP}"'"}}' \
                && echo "✓ dispatch sent — drova/prod-verify.yaml will run health-poll + auto-rollback"
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
