apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Canary via Argo Rollouts (aktiv):
  - rollout.yaml
  - analysis-template.yaml
  - service.yaml
  - service-canary.yaml
  - service-stable.yaml
  - hpa.yaml
  - pdb.yaml
  # Plain Deployment (Fallback — auskommentiert):
  # - deployment.yaml
