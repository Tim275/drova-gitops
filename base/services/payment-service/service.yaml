# Kafka-only Worker — exposed nur den Health-Port
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  labels:
    app: payment-service
    istio.io/use-waypoint: waypoint
spec:
  selector:
    app: payment-service
  ports:
    - name: health
      port: 8085
      targetPort: 8085
