apiVersion: v1
kind: Service
metadata:
  name: trip-service
  labels:
    istio.io/use-waypoint: waypoint
spec:
  selector:
    app: trip-service
  ports:
    - name: grpc
      port: 9093
      targetPort: 9093
