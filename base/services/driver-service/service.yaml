apiVersion: v1
kind: Service
metadata:
  name: driver-service
  labels:
    istio.io/use-waypoint: waypoint
spec:
  selector:
    app: driver-service
  ports:
    - name: grpc
      port: 9092
      targetPort: 9092
